# 📚 Полное руководство: Документация как Код (Docs as Code)
## Переход на GitHub для работы с документацией и AI

---

## 📖 Содержание

1. [Что такое Docs as Code](#что-такое-docs-as-code)
2. [Почему это важно в 2025 году](#почему-это-важно-в-2025-году)
3. [Основные принципы и философия](#основные-принципы-и-философия)
4. [Преимущества подхода](#преимущества-подхода)
5. [Инструменты экосистемы](#инструменты-экосистемы)
6. [Структура репозитория](#структура-репозитория)
7. [Статические генераторы сайтов](#статические-генераторы-сайтов)
8. [GitHub Actions: автоматизация](#github-actions-автоматизация)
9. [Интеграция с Claude AI](#интеграция-с-claude-ai)
10. [Пошаговый план внедрения](#пошаговый-план-внедрения)
11. [Best Practices](#best-practices)
12. [Примеры и ресурсы](#примеры-и-ресурсы)
13. [Решение типичных проблем](#решение-типичных-проблем)

---

## Что такое Docs as Code

### Определение

**Docs as Code (Документация как Код)** — это методология, при которой документация создается, хранится и управляется теми же инструментами и процессами, что и программный код.

### Ключевые характеристики

- **Простой текстовый формат** (обычно Markdown или AsciiDoc)
- **Версионный контроль** (Git)
- **Совместная работа** через Pull Requests
- **Автоматизация** через CI/CD пайплайны
- **Код рядом с документацией** в одном репозитории

### История подхода

Методология Docs as Code возникла как ответ на проблемы традиционных систем документирования:
- Документация часто отставала от кода
- Сложная синхронизация между разными платформами
- Отсутствие истории изменений
- Затрудненная совместная работа

---

## Почему это важно в 2025 году

### Современный контекст

1. **AI-революция**: AI-инструменты, такие как Claude, отлично работают с текстовыми форматами (Markdown)
2. **DevOps-культура**: Быстрые циклы разработки требуют синхронизированной документации
3. **Remote-работа**: Необходимость эффективной асинхронной коллаборации
4. **Открытый код**: GitHub стал стандартом для open-source проектов

### Статистика 2025 года

- **50% рост** вовлеченности контрибьюторов в проектах с хорошей документацией
- **30-45% ускорение** онбординга новых сотрудников
- **40% сокращение** циклов обратной связи при использовании PR для документации

---

## Основные принципы и философия

### 1. Документация = Код

Относитесь к документации как к полноценной части кодовой базы:
- Тот же уровень важности
- Те же стандарты качества
- Те же процессы ревью

### 2. Version Control First

Все изменения должны быть отслеживаемы:
```bash
# Каждое изменение документации коммитится
git add docs/api-guide.md
git commit -m "docs: add authentication flow diagram"
```

### 3. Single Source of Truth

Одно место для всей документации:
- Избегайте дублирования
- Используйте ссылки между документами
- Централизованное хранилище

### 4. Plain Text Forever

Используйте простые текстовые форматы:
- Читаемы человеком
- Работают в любом редакторе
- Легко индексируются поисковиками
- Поддерживаются всеми инструментами

### 5. Automation is Key

Автоматизируйте всё, что можно:
- Сборка документации
- Проверка ссылок
- Деплой на хостинг
- Уведомления о изменениях

---

## Преимущества подхода

### Для разработчиков

✅ **Привычные инструменты**: IDE, Git, GitHub
✅ **Контекст рядом**: Документация в том же репозитории, что и код
✅ **Быстрый цикл**: Пушнул изменения → автоматическая публикация
✅ **История изменений**: Полная прозрачность всех правок

### Для технических писателей

✅ **Мощные инструменты**: Расширения VS Code, автодополнение
✅ **Коллаборация**: Pull Requests с ревью и комментариями
✅ **Автоматизация**: CI/CD избавляет от рутины
✅ **Версионирование**: Легко поддерживать несколько версий документации

### Для команды в целом

✅ **Синхронизация**: Документация обновляется вместе с кодом
✅ **Прозрачность**: Все видят, что и когда менялось
✅ **Качество**: Код-ревью применяется и к документации
✅ **Доступность**: Документация всегда актуальна и доступна

### Для работы с AI (Claude)

✅ **Прямая интеграция**: Claude может напрямую работать с GitHub
✅ **Автоматическое обновление**: AI может создавать Pull Requests
✅ **Контекст**: AI видит всю историю и структуру проекта
✅ **Автоматизация**: AI может триггериться на события в репозитории

---

## Инструменты экосистемы

### Базовые инструменты

#### 1. **Git**
```bash
# Установка (если нужно)
# macOS
brew install git

# Ubuntu/Debian
sudo apt-get install git

# Windows (через Chocolatey)
choco install git
```

**Зачем нужен**: Версионный контроль, история изменений, ветвление

#### 2. **GitHub**
- Хостинг репозиториев
- Pull Requests для ревью
- Issues для трекинга задач
- Actions для автоматизации
- Pages для хостинга документации

#### 3. **Редакторы кода**

**VS Code** (рекомендуется)
```bash
# Установка расширений для Markdown
code --install-extension yzhang.markdown-all-in-one
code --install-extension DavidAnson.vscode-markdownlint
code --install-extension bierner.markdown-preview-github-styles
```

**Альтернативы**:
- Vim/Neovim с плагинами
- Sublime Text
- JetBrains IDE
- Obsidian (для личных заметок в Markdown)

### Языки разметки

#### Markdown (рекомендуется для начала)

**Плюсы**:
- Простой синтаксис
- Широкая поддержка
- Легко читается в исходном виде
- GitHub отображает автоматически

**Основной синтаксис**:
```markdown
# Заголовок 1
## Заголовок 2
### Заголовок 3

**Жирный текст**
*Курсив*

- Список
- Элемент

1. Нумерованный
2. Список

[Ссылка](https://example.com)

![Изображение](image.png)

`код inline`

```python
# Блок кода
def hello():
    print("Hello World")
```
```

#### AsciiDoc (для сложных проектов)

**Плюсы**:
- Более мощный синтаксис
- Лучше для технической документации
- Больше возможностей форматирования

**Минусы**:
- Сложнее в изучении
- Меньше инструментов поддержки

---

## Структура репозитория

### Базовая структура для документации

```
my-docs-repo/
├── .github/
│   ├── workflows/          # GitHub Actions workflows
│   │   ├── deploy-docs.yml
│   │   └── check-links.yml
│   ├── ISSUE_TEMPLATE/     # Шаблоны issues
│   │   ├── bug_report.md
│   │   └── feature_request.md
│   ├── PULL_REQUEST_TEMPLATE.md
│   └── CODEOWNERS          # Владельцы разделов документации
│
├── docs/                   # Основная документация
│   ├── index.md           # Главная страница
│   ├── getting-started/   # Руководство по началу работы
│   │   ├── installation.md
│   │   └── quickstart.md
│   ├── guides/            # Детальные гайды
│   │   ├── authentication.md
│   │   └── configuration.md
│   ├── reference/         # Справочная информация
│   │   ├── api.md
│   │   └── cli.md
│   ├── tutorials/         # Пошаговые туториалы
│   │   └── first-app.md
│   └── assets/            # Изображения, диаграммы
│       └── images/
│
├── examples/              # Примеры кода
│   └── sample-app/
│
├── .gitignore            # Игнорируемые файлы
├── README.md             # Главный README
├── CONTRIBUTING.md       # Руководство для контрибьюторов
├── LICENSE               # Лицензия
└── mkdocs.yml           # Конфигурация (если используете MkDocs)
```

### Расширенная структура (для больших проектов)

```
enterprise-docs/
├── .github/
│   └── workflows/
│       ├── deploy-docs.yml
│       ├── check-links.yml
│       ├── lint-markdown.yml
│       └── update-search-index.yml
│
├── docs/
│   ├── en/                # Английская версия
│   │   ├── v1.0/         # Версия 1.0
│   │   ├── v2.0/         # Версия 2.0
│   │   └── latest/       # Последняя версия
│   └── ru/               # Русская версия
│       └── latest/
│
├── scripts/              # Утилиты для работы с документацией
│   ├── build.sh
│   ├── check-links.py
│   └── generate-api-docs.js
│
├── themes/               # Кастомные темы
│   └── custom-theme/
│
└── tests/                # Тесты для документации
    └── link-checker.test.js
```

### Важные файлы

#### README.md
Это лицо вашего проекта на GitHub:

```markdown
# Название Проекта

Краткое описание (1-2 предложения)

## 🚀 Быстрый старт

[Ссылка на Getting Started](docs/getting-started.md)

## 📚 Документация

- [Руководство по установке](docs/installation.md)
- [API Reference](docs/api.md)
- [Примеры](examples/)

## 🤝 Контрибьюция

См. [CONTRIBUTING.md](CONTRIBUTING.md)

## 📝 Лицензия

[MIT](LICENSE)
```

#### CONTRIBUTING.md
Руководство для тех, кто хочет помочь:

```markdown
# Руководство по контрибьюции

## Как предложить изменения

1. Fork репозитория
2. Создайте ветку (`git checkout -b feature/amazing-docs`)
3. Внесите изменения
4. Закоммитьте (`git commit -m 'docs: add amazing section'`)
5. Push в ветку (`git push origin feature/amazing-docs`)
6. Откройте Pull Request

## Стиль документации

- Используйте Markdown
- Заголовки с заглавной буквы
- Короткие параграфы (2-3 предложения)
- Примеры кода для сложных концепций

## Структура коммитов

Используем [Conventional Commits](https://www.conventionalcommits.org/):

- `docs: описание` - изменения в документации
- `fix: описание` - исправление ошибок
- `feat: описание` - новая функциональность
```

#### CODEOWNERS
Определяет, кто ответственен за разделы:

```
# Файл CODEOWNERS
# Каждая линия - паттерн файлов и владельцы

# Вся документация
/docs/ @username @team-name

# API документация
/docs/reference/api.md @api-team

# Руководства для начинающих
/docs/getting-started/ @docs-team

# Все Markdown файлы
*.md @content-reviewers
```

---

## Статические генераторы сайтов

Статические генераторы превращают ваши Markdown файлы в красивые веб-сайты.

### Сравнительная таблица

| Генератор | Язык | Скорость | Сложность | Лучше для |
|-----------|------|----------|-----------|-----------|
| **MkDocs** | Python | Быстрый | Низкая | Документация проектов |
| **Docusaurus** | JavaScript | Средний | Средняя | Документация + блог |
| **Hugo** | Go | Очень быстрый | Средняя | Большие сайты |
| **Sphinx** | Python | Средний | Высокая | Техническая документация |
| **Jekyll** | Ruby | Средний | Средняя | GitHub Pages |

### 1. MkDocs (рекомендуется для начала)

#### Почему MkDocs?
- Очень простая настройка
- Отличная тема Material for MkDocs
- Live reload при разработке
- Идеально для документации

#### Установка

```bash
# Установка MkDocs
pip install mkdocs

# Установка Material темы
pip install mkdocs-material

# Создание нового проекта
mkdocs new my-project
cd my-project
```

#### Конфигурация (mkdocs.yml)

```yaml
site_name: Моя Документация
site_description: Описание проекта
site_author: Ваше Имя
site_url: https://username.github.io/project

# Тема
theme:
  name: material
  language: ru
  palette:
    - scheme: default
      primary: indigo
      accent: indigo
      toggle:
        icon: material/brightness-7
        name: Переключить на темную тему
    - scheme: slate
      primary: indigo
      accent: indigo
      toggle:
        icon: material/brightness-4
        name: Переключить на светлую тему
  features:
    - navigation.instant
    - navigation.tracking
    - navigation.tabs
    - navigation.sections
    - navigation.expand
    - navigation.top
    - search.suggest
    - search.highlight
    - content.code.copy

# Навигация
nav:
  - Главная: index.md
  - Начало работы:
    - Установка: getting-started/installation.md
    - Быстрый старт: getting-started/quickstart.md
  - Руководства:
    - Аутентификация: guides/authentication.md
    - Конфигурация: guides/configuration.md
  - Справка:
    - API: reference/api.md
    - CLI: reference/cli.md

# Плагины
plugins:
  - search:
      lang: 
        - ru
        - en
  - git-revision-date-localized:
      enable_creation_date: true

# Расширения Markdown
markdown_extensions:
  - pymdownx.highlight:
      anchor_linenums: true
  - pymdownx.superfences
  - pymdownx.tabbed:
      alternate_style: true
  - admonition
  - pymdownx.details
  - attr_list
  - md_in_html
  - pymdownx.emoji:
      emoji_index: !!python/name:material.extensions.emoji.twemoji
      emoji_generator: !!python/name:material.extensions.emoji.to_svg
```

#### Запуск и сборка

```bash
# Локальный сервер с live reload
mkdocs serve

# Сборка статического сайта
mkdocs build

# Деплой на GitHub Pages
mkdocs gh-deploy
```

### 2. Docusaurus (для современных проектов)

#### Почему Docusaurus?
- Современный React-based
- Отличная поддержка версионирования
- Встроенный блог
- Интерактивные компоненты

#### Установка

```bash
npx create-docusaurus@latest my-website classic
cd my-website
```

#### Конфигурация (docusaurus.config.js)

```javascript
module.exports = {
  title: 'Моя Документация',
  tagline: 'Описание проекта',
  url: 'https://username.github.io',
  baseUrl: '/project/',
  onBrokenLinks: 'throw',
  onBrokenMarkdownLinks: 'warn',
  favicon: 'img/favicon.ico',
  
  organizationName: 'username',
  projectName: 'project',
  
  i18n: {
    defaultLocale: 'ru',
    locales: ['ru', 'en'],
  },

  presets: [
    [
      'classic',
      {
        docs: {
          sidebarPath: require.resolve('./sidebars.js'),
          editUrl: 'https://github.com/username/project/edit/main/',
        },
        blog: {
          showReadingTime: true,
          editUrl: 'https://github.com/username/project/edit/main/',
        },
        theme: {
          customCss: require.resolve('./src/css/custom.css'),
        },
      },
    ],
  ],

  themeConfig: {
    navbar: {
      title: 'Моя Документация',
      items: [
        {
          type: 'doc',
          docId: 'intro',
          position: 'left',
          label: 'Документация',
        },
        {to: '/blog', label: 'Блог', position: 'left'},
        {
          href: 'https://github.com/username/project',
          label: 'GitHub',
          position: 'right',
        },
      ],
    },
    footer: {
      style: 'dark',
      links: [
        {
          title: 'Документация',
          items: [
            {
              label: 'Начало работы',
              to: '/docs/intro',
            },
          ],
        },
      ],
      copyright: `Copyright © ${new Date().getFullYear()} Ваше Имя.`,
    },
  },
};
```

#### Запуск

```bash
# Разработка
npm start

# Сборка
npm run build

# Деплой на GitHub Pages
GIT_USER=<Your GitHub username> npm run deploy
```

### 3. Hugo (для больших проектов)

#### Почему Hugo?
- Невероятная скорость сборки
- Мощная система шаблонов
- Отличная поддержка мультиязычности

#### Установка

```bash
# macOS
brew install hugo

# Ubuntu/Debian
sudo apt-get install hugo

# Windows
choco install hugo
```

#### Быстрый старт

```bash
# Создание нового сайта
hugo new site my-docs
cd my-docs

# Добавление темы (например, Hugo Book)
git init
git submodule add https://github.com/alex-shpak/hugo-book themes/book

# Конфигурация
echo 'theme = "book"' >> config.toml

# Создание контента
hugo new docs/getting-started.md

# Запуск сервера
hugo server
```

---

## GitHub Actions: автоматизация

GitHub Actions позволяет автоматизировать сборку, тестирование и деплой документации.

### Базовый workflow для MkDocs

Создайте файл `.github/workflows/deploy-docs.yml`:

```yaml
name: Deploy Documentation

# Когда запускается
on:
  push:
    branches:
      - main
    paths:
      - 'docs/**'
      - 'mkdocs.yml'
  pull_request:
    branches:
      - main
    paths:
      - 'docs/**'

# Разрешения
permissions:
  contents: write

# Задачи
jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    # Шаг 1: Получить код
    - name: Checkout repository
      uses: actions/checkout@v4
    
    # Шаг 2: Установить Python
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: 3.x
    
    # Шаг 3: Кэшировать зависимости
    - name: Cache dependencies
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-
    
    # Шаг 4: Установить зависимости
    - name: Install dependencies
      run: |
        pip install mkdocs-material
        pip install mkdocs-git-revision-date-localized-plugin
    
    # Шаг 5: Собрать и задеплоить документацию
    - name: Deploy to GitHub Pages
      run: mkdocs gh-deploy --force
```

### Проверка ссылок

Создайте `.github/workflows/check-links.yml`:

```yaml
name: Check Links

on:
  pull_request:
    paths:
      - 'docs/**'
  schedule:
    # Запускать каждую неделю в понедельник
    - cron: '0 0 * * 1'

jobs:
  check-links:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    
    - name: Check links
      uses: gaurav-nelson/github-action-markdown-link-check@v1
      with:
        use-quiet-mode: 'yes'
        config-file: '.github/workflows/link-check-config.json'
```

Конфиг для проверки ссылок `.github/workflows/link-check-config.json`:

```json
{
  "ignorePatterns": [
    {
      "pattern": "^http://localhost"
    },
    {
      "pattern": "^https://example.com"
    }
  ],
  "timeout": "20s",
  "retryOn429": true,
  "retryCount": 3
}
```

### Линтинг Markdown

Создайте `.github/workflows/lint-markdown.yml`:

```yaml
name: Lint Markdown

on:
  pull_request:
    paths:
      - '**.md'

jobs:
  markdownlint:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    
    - name: Run markdownlint
      uses: DavidAnson/markdownlint-cli2-action@v16
      with:
        globs: '**/*.md'
```

### Интеграция с Docusaurus

`.github/workflows/deploy-docusaurus.yml`:

```yaml
name: Deploy Docusaurus

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 18
        cache: npm
    
    - name: Install dependencies
      run: npm ci
    
    - name: Build website
      run: npm run build
    
    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./build
```

---

## Интеграция с Claude AI

### Прямая интеграция через Claude Code

Claude Code позволяет автоматизировать работу с документацией напрямую из GitHub.

#### Установка Claude GitHub App

1. **Установить Claude App для GitHub**:
   ```bash
   # В терминале с Claude Code
   /install-github-app
   ```

2. **Добавить API ключ в GitHub Secrets**:
   - Зайдите в Settings → Secrets and variables → Actions
   - Создайте новый secret: `ANTHROPIC_API_KEY`
   - Вставьте ваш API ключ

3. **Создать workflow файл** `.github/workflows/claude.yml`:

```yaml
name: Claude Documentation Assistant

on:
  issues:
    types: [opened, edited]
  issue_comment:
    types: [created]
  pull_request:
    types: [opened, synchronize]
  pull_request_review_comment:
    types: [created]

jobs:
  claude:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write
    
    steps:
    - name: Claude Code Action
      uses: anthropics/claude-code-action@v1
      with:
        anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
```

#### Использование

После настройки вы можете:

1. **Обновить документацию через Issue**:
   ```
   @claude обнови раздел API документации,
   добавь новый endpoint POST /users
   ```

2. **Ревью документации в PR**:
   ```
   @claude проверь правильность примеров кода
   в этом pull request
   ```

3. **Исправить ошибки**:
   ```
   @claude исправь битые ссылки в документации
   ```

### Рабочий процесс с Claude

#### 1. Создание нового раздела документации

**Issue**:
```markdown
Название: Добавить документацию по аутентификации

@claude создай новый раздел документации по аутентификации OAuth 2.0

Структура:
- Введение
- Получение токена
- Использование токена
- Обновление токена
- Примеры кода на Python и JavaScript
```

**Claude создаст PR** с новым файлом `docs/guides/oauth-authentication.md`

#### 2. Обновление существующих документов

**Комментарий в PR**:
```markdown
@claude обнови этот файл:
- Добавь примеры для TypeScript
- Улучши объяснение параметров
- Добавь диаграмму последовательности
```

#### 3. Автоматическая генерация API документации

**Issue**:
```markdown
@claude сгенерируй API документацию на основе
OpenAPI спецификации в файле openapi.yaml
```

### CLAUDE.md файл

Создайте файл `CLAUDE.md` в корне репозитория с инструкциями для Claude:

```markdown
# Инструкции для Claude

## Стиль документации

- Используй Markdown
- Все заголовки с заглавной буквы
- Короткие параграфы (2-3 предложения)
- Обязательные примеры кода для API

## Структура файлов

Все документы в папке `docs/`:
- `docs/guides/` - детальные руководства
- `docs/reference/` - справочная информация
- `docs/tutorials/` - пошаговые туториалы

## Примеры кода

Всегда включай примеры на:
- Python
- JavaScript
- cURL для API endpoints

## Ссылки

Используй относительные ссылки:
- `[API Reference](../reference/api.md)`
- Не `[API Reference](https://site.com/api)`

## Коммит сообщения

Формат: `docs: краткое описание изменения`

Примеры:
- `docs: add OAuth authentication guide`
- `docs: update API endpoints section`
- `docs: fix broken links in installation guide`
```

### Автоматизированные задачи

#### Еженедельное обновление документации

```yaml
# .github/workflows/weekly-docs-update.yml
name: Weekly Documentation Update

on:
  schedule:
    # Каждый понедельник в 9:00 UTC
    - cron: '0 9 * * 1'
  workflow_dispatch:  # Ручной запуск

jobs:
  update-docs:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    
    steps:
    - name: Create update issue
      uses: actions/github-script@v7
      with:
        script: |
          await github.rest.issues.create({
            owner: context.repo.owner,
            repo: context.repo.repo,
            title: 'Weekly Documentation Review',
            body: `@claude проверь документацию на:
            
            - [ ] Устаревшие ссылки
            - [ ] Неработающие примеры кода
            - [ ] Несоответствие с последней версией API
            - [ ] Опечатки и грамматические ошибки
            
            Создай PR с исправлениями.`
          })
```

---

## Пошаговый план внедрения

### Фаза 1: Подготовка (1-2 дня)

#### Шаг 1: Установка инструментов

```bash
# 1. Git
git --version  # Проверка установки

# 2. GitHub CLI
gh --version
# Если нет, установите: https://cli.github.com/

# 3. Python + MkDocs
python3 --version
pip install mkdocs mkdocs-material

# 4. Текстовый редактор
code --version  # VS Code
```

#### Шаг 2: Создание репозитория

```bash
# Локально
mkdir my-docs
cd my-docs
git init

# Или через GitHub
gh repo create my-docs --public --clone
cd my-docs
```

#### Шаг 3: Базовая структура

```bash
# Создание структуры папок
mkdir -p docs/{getting-started,guides,reference,tutorials}
mkdir -p docs/assets/images
mkdir -p .github/workflows

# Создание базовых файлов
touch README.md
touch CONTRIBUTING.md
touch LICENSE
touch docs/index.md
touch mkdocs.yml
```

### Фаза 2: Настройка (1 день)

#### Шаг 4: Конфигурация MkDocs

Создайте `mkdocs.yml`:

```yaml
site_name: Моя Документация

theme:
  name: material
  language: ru

nav:
  - Главная: index.md
  - Начало работы:
    - Установка: getting-started/installation.md
  - Руководства:
    - Первый проект: guides/first-project.md

markdown_extensions:
  - pymdownx.highlight
  - pymdownx.superfences
  - admonition
```

#### Шаг 5: Создание первого документа

`docs/index.md`:

```markdown
# Добро пожаловать в документацию

Это главная страница вашей документации.

## Быстрый старт

Начните с [установки](getting-started/installation.md).

## Руководства

- [Создание первого проекта](guides/first-project.md)
```

#### Шаг 6: Локальная проверка

```bash
# Запуск локального сервера
mkdocs serve

# Откройте http://localhost:8000
```

### Фаза 3: Автоматизация (1 день)

#### Шаг 7: GitHub Actions

Создайте `.github/workflows/deploy.yml`:

```yaml
name: Deploy Docs

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v5
      with:
        python-version: 3.x
    - run: pip install mkdocs-material
    - run: mkdocs gh-deploy --force
```

#### Шаг 8: Первый коммит и пуш

```bash
git add .
git commit -m "docs: initial documentation setup"
git branch -M main
git remote add origin https://github.com/username/my-docs.git
git push -u origin main
```

#### Шаг 9: Активация GitHub Pages

1. Зайдите в Settings → Pages
2. Source: Deploy from a branch
3. Branch: gh-pages
4. Сохраните

Документация будет доступна по адресу:
`https://username.github.io/my-docs/`

### Фаза 4: Интеграция с Claude (1 день)

#### Шаг 10: Установка Claude GitHub App

```bash
# В Claude терминале
/install-github-app
```

Следуйте инструкциям для:
- Установки приложения
- Добавления API ключа в Secrets

#### Шаг 11: Создание CLAUDE.md

```markdown
# Инструкции для Claude

## Стиль
- Markdown
- Короткие параграфы
- Примеры кода обязательны

## Структура
- docs/guides/ - руководства
- docs/reference/ - справка

## Коммиты
Формат: `docs: описание`
```

#### Шаг 12: Первая автоматизация

Создайте Issue:
```
@claude создай руководство по быстрому старту
в файле docs/getting-started/quickstart.md
```

### Фаза 5: Миграция контента (постепенно)

#### Шаг 13: Перенос из Google Drive

Для каждого документа:

1. Скачайте как Markdown (если возможно)
2. Или конвертируйте вручную
3. Поместите в соответствующую папку
4. Добавьте в навигацию mkdocs.yml

```bash
# Пример коммита
git add docs/guides/new-guide.md
git commit -m "docs: migrate guide from Google Drive"
git push
```

#### Шаг 14: Обновление ссылок

После миграции обновите все ссылки:

```markdown
<!-- Было -->
[Документ](https://docs.google.com/document/d/...)

<!-- Стало -->
[Документ](guides/document.md)
```

---

## Best Practices

### 1. Организация контента

#### Принцип Diátaxis

Документация должна быть четырех типов:

1. **Tutorials** (Туториалы):
   - Для обучения
   - Пошаговые инструкции
   - Ориентированы на новичков

2. **How-To Guides** (Руководства):
   - Решение конкретных задач
   - Для опытных пользователей
   - Практические примеры

3. **Reference** (Справка):
   - Техническое описание
   - API документация
   - Полнота важнее доступности

4. **Explanation** (Объяснения):
   - Концепции и теория
   - Контекст и история
   - Почему, а не как

#### Пример структуры

```
docs/
├── tutorials/           # "Учусь"
│   ├── your-first-app.md
│   └── advanced-usage.md
├── guides/             # "Решаю задачу"
│   ├── authentication.md
│   └── deployment.md
├── reference/          # "Справка"
│   ├── api.md
│   └── cli.md
└── explanation/        # "Понимаю"
    ├── architecture.md
    └── design-decisions.md
```

### 2. Написание качественного контента

#### Золотые правила

1. **Пишите для людей, не для машин**
   ```markdown
   ❌ Плохо: "Функция accept() используется для..."
   ✅ Хорошо: "Чтобы принять входящее соединение, вызовите accept()..."
   ```

2. **Используйте активный залог**
   ```markdown
   ❌ Плохо: "Файл может быть удален командой..."
   ✅ Хорошо: "Удалите файл командой..."
   ```

3. **Короткие параграфы**
   - Максимум 3-4 предложения
   - Одна идея на параграф

4. **Примеры обязательны**
   ```markdown
   ## Установка зависимостей
   
   Установите необходимые пакеты с помощью pip:
   
   ```bash
   pip install -r requirements.txt
   ```
   
   Проверьте установку:
   
   ```bash
   python -c "import flask; print(flask.__version__)"
   ```
   ```

#### Использование admonitions

```markdown
!!! note "Примечание"
    Это важная информация, на которую стоит обратить внимание.

!!! warning "Внимание"
    Это предупреждение о потенциальных проблемах.

!!! tip "Совет"
    Это полезный совет для оптимизации.

!!! danger "Опасно"
    Это может привести к потере данных или ошибкам.
```

### 3. Управление версиями документации

#### Стратегия версионирования

```
docs/
├── v1.0/
├── v2.0/
└── latest/ -> v2.0/  # Символическая ссылка
```

#### В mkdocs.yml

```yaml
plugins:
  - mike:
      version_selector: true
```

#### Команды mike

```bash
# Создать версию
mike deploy 1.0 latest -u

# Установить версию по умолчанию
mike set-default latest

# Список версий
mike list

# Удалить версию
mike delete 1.0
```

### 4. Работа в команде

#### Процесс ревью

1. **Создайте ветку**:
   ```bash
   git checkout -b docs/add-authentication-guide
   ```

2. **Внесите изменения**:
   - Пишите небольшими порциями
   - Коммитьте часто

3. **Откройте Pull Request**:
   - Четкое описание изменений
   - Ссылка на Issue (если есть)
   - Скриншоты (если применимо)

4. **Запросите ревью**:
   ```markdown
   ## Описание
   
   Добавлен гайд по OAuth аутентификации
   
   ## Изменения
   
   - Новый файл: `docs/guides/oauth.md`
   - Обновлена навигация в `mkdocs.yml`
   - Добавлены примеры кода
   
   ## Чеклист
   
   - [x] Проверены ссылки
   - [x] Примеры кода протестированы
   - [x] Нет опечаток
   
   Closes #123
   ```

#### Шаблон PR

`.github/PULL_REQUEST_TEMPLATE.md`:

```markdown
## Тип изменений

- [ ] Новая документация
- [ ] Обновление существующей документации
- [ ] Исправление ошибок
- [ ] Рефакторинг структуры

## Описание

Что именно было изменено и почему?

## Связанные Issue

Closes #

## Чеклист

- [ ] Документация соответствует стайл-гайду
- [ ] Все ссылки работают
- [ ] Примеры кода протестированы
- [ ] Нет опечаток и грамматических ошибок
- [ ] Обновлена навигация (если нужно)

## Скриншоты (если применимо)

<!-- Вставьте скриншоты новой/обновленной документации -->
```

### 5. Поддержание актуальности

#### Автоматические проверки

##### Проверка устаревших ссылок

```bash
# Установка инструмента
npm install -g markdown-link-check

# Создание конфига .markdown-link-check.json
{
  "timeout": "20s",
  "retryOn429": true,
  "retryCount": 3,
  "ignorePatterns": [
    {
      "pattern": "^http://localhost"
    }
  ]
}

# Запуск проверки
find docs -name "*.md" -exec markdown-link-check {} \;
```

##### GitHub Action для проверки

```yaml
name: Check Links Weekly

on:
  schedule:
    - cron: '0 0 * * 1'  # Каждый понедельник
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: gaurav-nelson/github-action-markdown-link-check@v1
```

#### Регулярный аудит

Создайте Issue template `.github/ISSUE_TEMPLATE/docs-audit.md`:

```markdown
---
name: Ежемесячный аудит документации
about: Регулярная проверка актуальности
title: 'Docs Audit: [месяц год]'
labels: documentation, audit
---

## Чеклист аудита

### Общее
- [ ] Все ссылки работают
- [ ] Нет устаревшей информации
- [ ] Версии ПО актуальны

### Контент
- [ ] Примеры кода работают
- [ ] Скриншоты актуальны
- [ ] API reference соответствует последней версии

### Структура
- [ ] Навигация логична
- [ ] Нет дублирования информации
- [ ] Поиск работает корректно

## Найденные проблемы

<!-- Список проблем с описанием -->

## План действий

<!-- Что нужно исправить -->
```

### 6. Стиль написания

#### Стайл-гайд

Создайте файл `STYLE_GUIDE.md`:

```markdown
# Стайл-гайд документации

## Форматирование

### Заголовки
- H1 (`#`) - только один на страницу (название документа)
- H2 (`##`) - основные разделы
- H3 (`###`) - подразделы
- Не используйте H4-H6 без необходимости

### Списки
- Используйте `-` для ненумерованных списков
- Используйте `1.` для нумерованных списков
- Отступ - 2 пробела для вложенности

### Код
- Всегда указывайте язык в блоках кода:
  ````markdown
  ```python
  print("Hello")
  ```
  ````
- Inline код обрамляйте backticks: `code`

## Терминология

| Используйте | Вместо |
|-------------|--------|
| нажмите | кликните |
| запустите | запустить |
| выберите | выбрать |

## Примеры

### Хороший пример

```markdown
## Установка

Установите пакет с помощью pip:

```bash
pip install mypackage
```

Проверьте установку:

```bash
python -c "import mypackage; print(mypackage.__version__)"
```
```

### Плохой пример

```markdown
## Установка
Установите: pip install mypackage
```
```

---

## Примеры и ресурсы

### Отличные примеры Docs as Code

#### 1. **Python Documentation**
- **URL**: https://docs.python.org
- **Генератор**: Sphinx
- **Исходники**: https://github.com/python/cpython/tree/main/Doc
- **Что посмотреть**: Структура, версионирование, мультиязычность

#### 2. **Vue.js Documentation**
- **URL**: https://vuejs.org/guide/
- **Генератор**: VitePress
- **Исходники**: https://github.com/vuejs/docs
- **Что посмотреть**: Интерактивные примеры, структура контента

#### 3. **Kubernetes Documentation**
- **URL**: https://kubernetes.io/docs/
- **Генератор**: Hugo
- **Исходники**: https://github.com/kubernetes/website
- **Что посмотреть**: Масштаб, мультиязычность, процессы контрибьюции

#### 4. **FastAPI Documentation**
- **URL**: https://fastapi.tiangolo.com
- **Генератор**: MkDocs Material
- **Исходники**: https://github.com/tiangolo/fastapi
- **Что посмотреть**: Отличные примеры, навигация, стиль

#### 5. **Stripe API Documentation**
- **URL**: https://stripe.com/docs
- **Генератор**: Кастомное решение
- **Что посмотреть**: Интерактивность, примеры на разных языках

### Полезные ресурсы

#### Книги

1. **"Docs Like Code" by Anne Gentle**
   - Издание: 2nd Edition (2022)
   - Полное руководство по Docs as Code
   - URL: https://www.docslikecode.com/book/

2. **"The Product is Docs" by Splunk**
   - Бесплатная книга о документировании
   - URL: https://www.splunk.com/en_us/blog/tips-and-tricks/the-product-is-docs.html

#### Курсы и туториалы

1. **Write the Docs**
   - URL: https://www.writethedocs.org/guide/
   - Комьюнити и ресурсы для технических писателей

2. **GitHub Skills**
   - URL: https://skills.github.com/
   - Интерактивные курсы по GitHub

3. **MkDocs Tutorial**
   - URL: https://www.mkdocs.org/getting-started/
   - Официальная документация MkDocs

#### Инструменты

1. **Vale** - Линтер для прозы
   ```bash
   brew install vale
   vale docs/
   ```

2. **Grammarly** - Проверка грамматики
   - Расширение для браузера
   - Интеграция с VS Code

3. **Draw.io** - Диаграммы
   - URL: https://app.diagrams.net/
   - Можно интегрировать в Markdown

4. **Excalidraw** - Рисунки от руки
   - URL: https://excalidraw.com/
   - Минималистичные диаграммы

#### Коммьюнити

1. **Write the Docs Slack**
   - URL: https://www.writethedocs.org/slack/
   - Активное сообщество technical writers

2. **r/technicalwriting на Reddit**
   - URL: https://reddit.com/r/technicalwriting
   - Обсуждения и советы

---

## Решение типичных проблем

### Проблема: Медленная сборка документации

**Причина**: Большое количество файлов или сложная конфигурация

**Решения**:

1. **Используйте Hugo** вместо MkDocs для больших проектов:
   ```bash
   # Hugo собирает 1000+ страниц за секунды
   hugo server
   ```

2. **Включите кэширование** в GitHub Actions:
   ```yaml
   - name: Cache dependencies
     uses: actions/cache@v3
     with:
       path: ~/.cache/pip
       key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
   ```

3. **Используйте incremental builds**:
   ```yaml
   # В mkdocs.yml
   plugins:
     - search
     - git-revision-date-localized:
         enable_creation_date: false  # Отключить для скорости
   ```

### Проблема: Конфликты при мерже

**Причина**: Несколько человек редактируют один файл

**Решения**:

1. **Используйте CODEOWNERS**:
   ```
   # .github/CODEOWNERS
   /docs/api/ @api-team
   /docs/guides/ @docs-team
   ```

2. **Создавайте маленькие, фокусированные PR**:
   - Один PR = одна тема
   - Избегайте массовых рефакторингов

3. **Регулярно обновляйте ветки**:
   ```bash
   git fetch origin
   git rebase origin/main
   ```

### Проблема: Устаревшая документация

**Причина**: Отсутствие процессов поддержки

**Решения**:

1. **Автоматические напоминания**:
   ```yaml
   # .github/workflows/docs-reminder.yml
   name: Monthly Docs Review
   on:
     schedule:
       - cron: '0 9 1 * *'  # 1-го числа каждого месяца
   
   jobs:
     remind:
       runs-on: ubuntu-latest
       steps:
       - name: Create Issue
         uses: actions/github-script@v7
         with:
           script: |
             github.rest.issues.create({
               owner: context.repo.owner,
               repo: context.repo.repo,
               title: 'Monthly Documentation Review',
               body: 'Время проверить документацию!',
               labels: ['documentation']
             })
   ```

2. **Definition of Done** включает документацию:
   - PR не мержится без обновления docs
   - Review checklist включает проверку документации

3. **Дата последнего обновления**:
   ```yaml
   # В mkdocs.yml
   plugins:
     - git-revision-date-localized:
         enable_creation_date: true
   ```

### Проблема: Сложно найти нужную информацию

**Причина**: Плохая структура или навигация

**Решения**:

1. **Улучшите поиск**:
   ```yaml
   # mkdocs.yml для MkDocs Material
   plugins:
     - search:
         separator: '[\s\-\.]+'
         lang:
           - en
           - ru
   ```

2. **Добавьте таблицу содержания** на каждую страницу:
   ```markdown
   # Название страницы
   
   ## Содержание
   
   - [Раздел 1](#раздел-1)
   - [Раздел 2](#раздел-2)
   ```

3. **Создайте sitemap**:
   - Автоматически генерируется большинством SSG
   - Помогает поисковикам индексировать

### Проблема: Миграция с Google Drive занимает слишком много времени

**Причина**: Много документов, ручная конвертация

**Решения**:

1. **Инкрементальная миграция**:
   - Начните с самых важных документов
   - Мигрируйте по одному разделу за раз
   - Оставьте временные ссылки на Google Drive

2. **Используйте инструменты конвертации**:
   ```bash
   # Pandoc для конвертации
   pandoc -f docx -t markdown input.docx -o output.md
   
   # Google Docs to Markdown (расширение Chrome)
   # https://workspace.google.com/marketplace/app/docs_to_markdown/700168918607
   ```

3. **Попросите Claude помочь**:
   ```
   @claude конвертируй содержимое этого Google Docs
   документа в Markdown и создай файл в docs/guides/
   ```

---

## Чеклист готовности к переходу

Используйте этот чеклист, чтобы убедиться, что вы готовы к миграции на Docs as Code:

### Технические требования
- [ ] Git установлен и настроен
- [ ] GitHub аккаунт создан
- [ ] Выбран статический генератор (MkDocs/Docusaurus/Hugo)
- [ ] Настроен текстовый редактор с поддержкой Markdown
- [ ] GitHub CLI установлен (опционально)

### Структура проекта
- [ ] Создан репозиторий на GitHub
- [ ] Настроена базовая структура папок
- [ ] Созданы основные файлы (README, CONTRIBUTING, LICENSE)
- [ ] Настроен конфигурационный файл SSG
- [ ] Создан первый документ для проверки

### Автоматизация
- [ ] Настроен GitHub Actions для деплоя
- [ ] Проверка ссылок работает автоматически
- [ ] Линтинг Markdown настроен
- [ ] GitHub Pages активирован
- [ ] Домен настроен (если нужен)

### Интеграция с Claude
- [ ] Claude GitHub App установлен
- [ ] API ключ добавлен в Secrets
- [ ] Создан workflow файл для Claude
- [ ] CLAUDE.md файл создан с инструкциями
- [ ] Протестирован простой запрос к Claude

### Процессы
- [ ] Определена структура документации (Diátaxis или другая)
- [ ] Созданы шаблоны для Issue и PR
- [ ] Написан стайл-гайд
- [ ] Определены процессы ревью
- [ ] Назначены ответственные (CODEOWNERS)

### Миграция контента
- [ ] Составлен список документов для миграции
- [ ] Определена последовательность миграции
- [ ] Выбран метод конвертации (ручной/автоматический)
- [ ] Определена стратегия для битых ссылок
- [ ] План обновления внешних ссылок на документацию

---

## Заключение

### Ключевые моменты

1. **Docs as Code - это не просто инструмент**, это философия и методология работы с документацией
2. **Начните с малого**: Не обязательно сразу мигрировать всё
3. **Автоматизация - ваш друг**: CI/CD избавляет от рутины
4. **GitHub + Claude = мощная комбинация** для современной документации
5. **Качество важнее количества**: Лучше 10 отличных документов, чем 100 посредственных

### Следующие шаги

1. **Прочитайте этот гайд** полностью 📖
2. **Начните с пилотного проекта** 🚀
3. **Настройте автоматизацию** ⚙️
4. **Интегрируйте Claude** 🤖
5. **Постепенно мигрируйте контент** 📦
6. **Наслаждайтесь результатом** 🎉

### Дополнительная помощь

Если у вас возникли вопросы:

1. **GitHub Discussions** вашего репозитория
2. **Write the Docs** сообщество
3. **Stack Overflow** с тегом `documentation`
4. **Claude AI** - просто спросите! 😉

---

## Приложения

### Приложение A: Полный пример mkdocs.yml

```yaml
# Информация о сайте
site_name: Моя Документация
site_description: Подробная документация проекта
site_author: Ваше Имя
site_url: https://username.github.io/project

# Репозиторий
repo_name: username/project
repo_url: https://github.com/username/project
edit_uri: edit/main/docs/

# Копирайт
copyright: Copyright &copy; 2025 Ваше Имя

# Тема
theme:
  name: material
  language: ru
  
  # Цветовая схема
  palette:
    - scheme: default
      primary: indigo
      accent: indigo
      toggle:
        icon: material/brightness-7
        name: Переключить на темную тему
    - scheme: slate
      primary: indigo
      accent: indigo
      toggle:
        icon: material/brightness-4
        name: Переключить на светлую тему
  
  # Иконка
  icon:
    logo: material/book-open-page-variant
    repo: fontawesome/brands/github
  
  # Возможности
  features:
    - navigation.instant        # Мгновенная загрузка
    - navigation.tracking       # Отслеживание якорей в URL
    - navigation.tabs           # Вкладки навигации
    - navigation.sections       # Секции в боковой панели
    - navigation.expand         # Развернутая навигация по умолчанию
    - navigation.top            # Кнопка "Наверх"
    - navigation.footer         # Навигация в футере
    - search.suggest            # Подсказки при поиске
    - search.highlight          # Подсветка результатов поиска
    - search.share              # Кнопка "Поделиться" для поиска
    - content.code.copy         # Кнопка копирования кода
    - content.code.annotate     # Аннотации в коде
    - content.tabs.link         # Связанные табы

# Навигация
nav:
  - Главная: index.md
  - Начало работы:
    - Установка: getting-started/installation.md
    - Быстрый старт: getting-started/quickstart.md
    - Конфигурация: getting-started/configuration.md
  - Руководства:
    - Аутентификация: guides/authentication.md
    - База данных: guides/database.md
    - API интеграции: guides/api.md
  - Справочник:
    - API Reference: reference/api.md
    - CLI Reference: reference/cli.md
    - Переменные окружения: reference/environment.md
  - Туториалы:
    - Первое приложение: tutorials/first-app.md
    - Продвинутое использование: tutorials/advanced.md
  - О проекте:
    - Changelog: about/changelog.md
    - Contributing: about/contributing.md
    - License: about/license.md

# Плагины
plugins:
  - search:
      separator: '[\s\-\.]+'
      lang:
        - en
        - ru
  - git-revision-date-localized:
      enable_creation_date: true
      type: timeago
  - minify:
      minify_html: true

# Расширения Markdown
markdown_extensions:
  # Syntax highlighting
  - pymdownx.highlight:
      anchor_linenums: true
      line_spans: __span
      pygments_lang_class: true
  - pymdownx.inlinehilite
  - pymdownx.snippets
  - pymdownx.superfences:
      custom_fences:
        - name: mermaid
          class: mermaid
          format: !!python/name:pymdownx.superfences.fence_code_format
  
  # Tabs
  - pymdownx.tabbed:
      alternate_style: true
  
  # Admonitions
  - admonition
  - pymdownx.details
  
  # Lists
  - def_list
  - pymdownx.tasklist:
      custom_checkbox: true
  
  # Formatting
  - pymdownx.critic
  - pymdownx.caret
  - pymdownx.keys
  - pymdownx.mark
  - pymdownx.tilde
  
  # Attributes
  - attr_list
  - md_in_html
  
  # Emoji
  - pymdownx.emoji:
      emoji_index: !!python/name:material.extensions.emoji.twemoji
      emoji_generator: !!python/name:material.extensions.emoji.to_svg
  
  # Tables
  - tables
  
  # Footnotes
  - footnotes
  
  # Abbreviations
  - abbr

# Дополнительно
extra:
  social:
    - icon: fontawesome/brands/github
      link: https://github.com/username
    - icon: fontawesome/brands/twitter
      link: https://twitter.com/username
  analytics:
    provider: google
    property: G-XXXXXXXXXX
  version:
    provider: mike
```

### Приложение B: Примеры коммит сообщений

Следуйте [Conventional Commits](https://www.conventionalcommits.org/):

```bash
# Новая документация
git commit -m "docs: add OAuth authentication guide"

# Обновление существующей
git commit -m "docs: update API endpoints section"

# Исправление ошибок
git commit -m "docs: fix broken links in installation guide"

# Рефакторинг
git commit -m "docs: reorganize getting started section"

# Добавление примеров
git commit -m "docs: add Python code examples to API reference"

# Breaking changes
git commit -m "docs!: remove deprecated v1 API documentation"
```

### Приложение C: Markdown шпаргалка

```markdown
# Заголовок 1
## Заголовок 2
### Заголовок 3

**Жирный**
*Курсив*
***Жирный курсив***
~~Зачеркнутый~~

- Список
  - Вложенный
    - Еще вложенный

1. Нумерованный
2. Список
   1. Вложенный

- [ ] Checkbox
- [x] Выполнено

[Ссылка](https://example.com)
[Ссылка с title](https://example.com "Title")
[Относительная ссылка](./page.md)
[Якорь](#section)

![Alt text](image.png)
![Изображение с title](image.png "Image title")

`inline code`

```python
# Блок кода
def hello():
    print("Hello World")
```

| Колонка 1 | Колонка 2 |
|-----------|-----------|
| Ячейка 1  | Ячейка 2  |

> Цитата
> Многострочная
> цитата

---

Горизонтальная линия

Footnote[^1]

[^1]: Текст сноски

*[HTML]: HyperText Markup Language
Аббревиатура: HTML

!!! note
    Admonition блок

!!! warning "Внимание!"
    С кастомным заголовком
```

---

**Версия документа**: 1.0  
**Дата создания**: Ноябрь 2025  
**Автор**: Claude AI  
**Лицензия**: Свободное использование и распространение
